---
detectors:
  UncommunicativeVariableName:
    exclude:
      - CreateUsers#change
      - CreateLists#change
      - CreateItems#change

  UncommunicativeModuleName:
    reject:
      - "/^ListApp$/"

exclude_paths:
  - vendor
  - db/migrate
